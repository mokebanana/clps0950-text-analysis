function [countEmotion] = sumEmotion(bagTbl,)
    countEmotion = zeros(1,10);
    table_size = bagTbl.size();
    rows = table_size(1);
    for i = 1:rows
        row = bagTbl(i,:);
        word = bagTbl(i:1);
        if isequal(word, )

        end
    end
end